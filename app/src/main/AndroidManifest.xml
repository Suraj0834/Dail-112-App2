<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- Camera feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".Dial112Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dial112"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Splash Activity -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Dial112.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Citizen Home Activity -->
        <activity
            android:name=".ui.CitizenHomeActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- Police Home Activity -->
        <activity
            android:name=".ui.PoliceHomeActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <!-- SOS Activity -->
        <activity
            android:name=".ui.SOSActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Chat Activity -->
        <activity
            android:name=".ui.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Camera Scanner Activity -->
        <activity
            android:name=".ui.CameraScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Signup Activity -->
        <activity
            android:name=".ui.SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- OTP Verification Activity -->
        <activity
            android:name=".ui.OTPVerificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Profile Settings Activity -->
        <activity
            android:name=".ui.ProfileSettingsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Case History Activity -->
        <activity
            android:name=".ui.CaseHistoryActivity"
            android:exported="false" />

        <!-- Case Details Activity -->
        <activity
            android:name=".ui.CaseDetailsActivity"
            android:exported="false" />

        <!-- Case Management Activity (Police) -->
        <activity
            android:name=".ui.CaseManagementActivity"
            android:exported="false" />

        <!-- Notifications Activity -->
        <activity
            android:name=".ui.NotificationsActivity"
            android:exported="false" />

        <!-- Map Location Activity -->
        <activity
            android:name=".ui.MapLocationActivity"
            android:exported="false" />

        <!-- Emergency Contacts Activity -->
        <activity
            android:name=".ui.EmergencyContactsActivity"
            android:exported="false" />

        <!-- Help & FAQ Activity -->
        <activity
            android:name=".ui.HelpFAQActivity"
            android:exported="false" />

        <!-- Feedback Activity -->
        <activity
            android:name=".ui.FeedbackActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Admin Dashboard Activity -->
        <activity
            android:name=".ui.AdminDashboardActivity"
            android:exported="false" />

        <!-- SOS Foreground Service -->
        <service
            android:name=".service.SOSForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
